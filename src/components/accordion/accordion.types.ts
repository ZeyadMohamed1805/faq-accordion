/** According Types **/

// Accordion Props Type
export type AccordionPropsType =
{
    headingTitle: string;
    bodyContent: string;
};

// AccordionStateType
export type AccordionStateType = 'open' | 'closed';